import 'package:flutter/material.dart';

class AppShadows {
  const AppShadows._();

  static const elevatedDark = [
    BoxShadow(color: Color(0x55242A35), blurRadius: 28, offset: Offset(0, 18)),
  ];

  static const elevatedLight = [
    BoxShadow(color: Color(0x140F172A), blurRadius: 26, offset: Offset(0, 16)),
  ];
}
